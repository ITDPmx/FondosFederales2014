(function(){
	$('#acerca').modal();
	
});    

/*	var language = window.navigator.userLanguage || navigator.language;
	console.log(language);*/

/*L.DomEvent.addListener(estado, 'change', function (e) {
	var valores = JSON.parse(e.target.value);
	console.log(valores);
	for (var i in valores) {
		console.log(i+valores[i]);
		
	};
	info.update(e);
	L.DomEvent.stopPropagation(e);
});*/